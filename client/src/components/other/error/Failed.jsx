import React, { Component } from 'react';

class Failed extends Component {
    render () {
        return (
            <h1>Failed Loading Blockchain !</h1>
        );
    };
};

export default Failed;
